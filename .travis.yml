language: objective-c
osx_image: xcode7.3 
xcode_workspace: AKTrakt.xcworkspace
xcode_scheme: tvOS
before_install:
  - gem update cocoapods
  - pod repo update
script:
  - xcodebuild -workspace AKTrakt.xcworkspace -scheme tvOS -sdk appletvos9.2 CODE_SIGNING_REQUIRED=NO
